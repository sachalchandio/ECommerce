nameVar  = Params_joinbrand[:name]

for loop array_sub_ids[]= [1,2,4,5,6] do |x|
    @instance = MODELjoin_on_d_sd_b.new()
    @instance.name = nameVar
    @instance.sub_deptID = x
    
   
                MODEL.rb (UNIQUE {:brand_id, :subDeptID})
                
                validates_uniqueness_of :teacher_id, :scope => [:semester_id, :class_id]
    2    1
    2    2
    2    3
    2    4
    2    5
    2    6
    
    
    new_array = params[:brand][:sub_department_ids]
        new_array = new_array[1..-1].map(&:to_i)
        @brand.sub_department_ids = new_array